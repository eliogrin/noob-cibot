<beans:beans xmlns="http://www.springframework.org/schema/security"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://www.springframework.org/schema/beans
                 http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
                 http://www.springframework.org/schema/security
                 http://www.springframework.org/schema/security/spring-security-4.0.xsd">

    <!-- Global Security Settings -->
    <global-method-security pre-post-annotations="enabled"/>

    <!--Security for rest-->
    <http pattern="/rest/**" security="none"/>

    <!--Security for Web-->
    <http pattern="/**" use-expressions="true" disable-url-rewriting="true">

        <session-management invalid-session-url="/login">
            <!--<concurrency-control max-sessions="1" error-if-maximum-exceeded="true" expired-url="/login?error"/>-->
        </session-management>

        <form-login login-page="/login" authentication-failure-url="/login?error"
                    login-processing-url="/login" default-target-url="/" always-use-default-target="true"
                    username-parameter="username" password-parameter="password"/>

        <logout logout-url="/logout" logout-success-url="/login" delete-cookies="JSESSIONID" invalidate-session="true"/>
        <intercept-url pattern="/login*" access="permitAll()"/>
        <intercept-url pattern="/" access="hasRole('ROLE_ADMIN')"/>
    </http>

    <!-- Authentication Providers for Form Login -->
    <authentication-manager alias="webAuthenticationManager">
        <authentication-provider user-service-ref="webLoginService">
            <password-encoder hash="sha-256"/>
        </authentication-provider>
    </authentication-manager>
</beans:beans>